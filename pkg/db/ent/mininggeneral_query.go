// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/ledger-middleware/pkg/db/ent/mininggeneral"
	"github.com/NpoolPlatform/ledger-middleware/pkg/db/ent/predicate"
	"github.com/google/uuid"
)

// MiningGeneralQuery is the builder for querying MiningGeneral entities.
type MiningGeneralQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.MiningGeneral
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MiningGeneralQuery builder.
func (mgq *MiningGeneralQuery) Where(ps ...predicate.MiningGeneral) *MiningGeneralQuery {
	mgq.predicates = append(mgq.predicates, ps...)
	return mgq
}

// Limit adds a limit step to the query.
func (mgq *MiningGeneralQuery) Limit(limit int) *MiningGeneralQuery {
	mgq.limit = &limit
	return mgq
}

// Offset adds an offset step to the query.
func (mgq *MiningGeneralQuery) Offset(offset int) *MiningGeneralQuery {
	mgq.offset = &offset
	return mgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mgq *MiningGeneralQuery) Unique(unique bool) *MiningGeneralQuery {
	mgq.unique = &unique
	return mgq
}

// Order adds an order step to the query.
func (mgq *MiningGeneralQuery) Order(o ...OrderFunc) *MiningGeneralQuery {
	mgq.order = append(mgq.order, o...)
	return mgq
}

// First returns the first MiningGeneral entity from the query.
// Returns a *NotFoundError when no MiningGeneral was found.
func (mgq *MiningGeneralQuery) First(ctx context.Context) (*MiningGeneral, error) {
	nodes, err := mgq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mininggeneral.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mgq *MiningGeneralQuery) FirstX(ctx context.Context) *MiningGeneral {
	node, err := mgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MiningGeneral ID from the query.
// Returns a *NotFoundError when no MiningGeneral ID was found.
func (mgq *MiningGeneralQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mgq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mininggeneral.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mgq *MiningGeneralQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := mgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MiningGeneral entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MiningGeneral entity is found.
// Returns a *NotFoundError when no MiningGeneral entities are found.
func (mgq *MiningGeneralQuery) Only(ctx context.Context) (*MiningGeneral, error) {
	nodes, err := mgq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mininggeneral.Label}
	default:
		return nil, &NotSingularError{mininggeneral.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mgq *MiningGeneralQuery) OnlyX(ctx context.Context) *MiningGeneral {
	node, err := mgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MiningGeneral ID in the query.
// Returns a *NotSingularError when more than one MiningGeneral ID is found.
// Returns a *NotFoundError when no entities are found.
func (mgq *MiningGeneralQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = mgq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mininggeneral.Label}
	default:
		err = &NotSingularError{mininggeneral.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mgq *MiningGeneralQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := mgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MiningGenerals.
func (mgq *MiningGeneralQuery) All(ctx context.Context) ([]*MiningGeneral, error) {
	if err := mgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return mgq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (mgq *MiningGeneralQuery) AllX(ctx context.Context) []*MiningGeneral {
	nodes, err := mgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MiningGeneral IDs.
func (mgq *MiningGeneralQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := mgq.Select(mininggeneral.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mgq *MiningGeneralQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := mgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mgq *MiningGeneralQuery) Count(ctx context.Context) (int, error) {
	if err := mgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return mgq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (mgq *MiningGeneralQuery) CountX(ctx context.Context) int {
	count, err := mgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mgq *MiningGeneralQuery) Exist(ctx context.Context) (bool, error) {
	if err := mgq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return mgq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (mgq *MiningGeneralQuery) ExistX(ctx context.Context) bool {
	exist, err := mgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MiningGeneralQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mgq *MiningGeneralQuery) Clone() *MiningGeneralQuery {
	if mgq == nil {
		return nil
	}
	return &MiningGeneralQuery{
		config:     mgq.config,
		limit:      mgq.limit,
		offset:     mgq.offset,
		order:      append([]OrderFunc{}, mgq.order...),
		predicates: append([]predicate.MiningGeneral{}, mgq.predicates...),
		// clone intermediate query.
		sql:    mgq.sql.Clone(),
		path:   mgq.path,
		unique: mgq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MiningGeneral.Query().
//		GroupBy(mininggeneral.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (mgq *MiningGeneralQuery) GroupBy(field string, fields ...string) *MiningGeneralGroupBy {
	grbuild := &MiningGeneralGroupBy{config: mgq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := mgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return mgq.sqlQuery(ctx), nil
	}
	grbuild.label = mininggeneral.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.MiningGeneral.Query().
//		Select(mininggeneral.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (mgq *MiningGeneralQuery) Select(fields ...string) *MiningGeneralSelect {
	mgq.fields = append(mgq.fields, fields...)
	selbuild := &MiningGeneralSelect{MiningGeneralQuery: mgq}
	selbuild.label = mininggeneral.Label
	selbuild.flds, selbuild.scan = &mgq.fields, selbuild.Scan
	return selbuild
}

func (mgq *MiningGeneralQuery) prepareQuery(ctx context.Context) error {
	for _, f := range mgq.fields {
		if !mininggeneral.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mgq.path != nil {
		prev, err := mgq.path(ctx)
		if err != nil {
			return err
		}
		mgq.sql = prev
	}
	if mininggeneral.Policy == nil {
		return errors.New("ent: uninitialized mininggeneral.Policy (forgotten import ent/runtime?)")
	}
	if err := mininggeneral.Policy.EvalQuery(ctx, mgq); err != nil {
		return err
	}
	return nil
}

func (mgq *MiningGeneralQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MiningGeneral, error) {
	var (
		nodes = []*MiningGeneral{}
		_spec = mgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*MiningGeneral).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &MiningGeneral{config: mgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mgq.modifiers) > 0 {
		_spec.Modifiers = mgq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mgq *MiningGeneralQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mgq.querySpec()
	if len(mgq.modifiers) > 0 {
		_spec.Modifiers = mgq.modifiers
	}
	_spec.Node.Columns = mgq.fields
	if len(mgq.fields) > 0 {
		_spec.Unique = mgq.unique != nil && *mgq.unique
	}
	return sqlgraph.CountNodes(ctx, mgq.driver, _spec)
}

func (mgq *MiningGeneralQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := mgq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (mgq *MiningGeneralQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   mininggeneral.Table,
			Columns: mininggeneral.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: mininggeneral.FieldID,
			},
		},
		From:   mgq.sql,
		Unique: true,
	}
	if unique := mgq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := mgq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mininggeneral.FieldID)
		for i := range fields {
			if fields[i] != mininggeneral.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mgq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mgq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mgq *MiningGeneralQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mgq.driver.Dialect())
	t1 := builder.Table(mininggeneral.Table)
	columns := mgq.fields
	if len(columns) == 0 {
		columns = mininggeneral.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mgq.sql != nil {
		selector = mgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mgq.unique != nil && *mgq.unique {
		selector.Distinct()
	}
	for _, m := range mgq.modifiers {
		m(selector)
	}
	for _, p := range mgq.predicates {
		p(selector)
	}
	for _, p := range mgq.order {
		p(selector)
	}
	if offset := mgq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mgq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (mgq *MiningGeneralQuery) ForUpdate(opts ...sql.LockOption) *MiningGeneralQuery {
	if mgq.driver.Dialect() == dialect.Postgres {
		mgq.Unique(false)
	}
	mgq.modifiers = append(mgq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return mgq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (mgq *MiningGeneralQuery) ForShare(opts ...sql.LockOption) *MiningGeneralQuery {
	if mgq.driver.Dialect() == dialect.Postgres {
		mgq.Unique(false)
	}
	mgq.modifiers = append(mgq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return mgq
}

// MiningGeneralGroupBy is the group-by builder for MiningGeneral entities.
type MiningGeneralGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mggb *MiningGeneralGroupBy) Aggregate(fns ...AggregateFunc) *MiningGeneralGroupBy {
	mggb.fns = append(mggb.fns, fns...)
	return mggb
}

// Scan applies the group-by query and scans the result into the given value.
func (mggb *MiningGeneralGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := mggb.path(ctx)
	if err != nil {
		return err
	}
	mggb.sql = query
	return mggb.sqlScan(ctx, v)
}

func (mggb *MiningGeneralGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range mggb.fields {
		if !mininggeneral.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := mggb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mggb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (mggb *MiningGeneralGroupBy) sqlQuery() *sql.Selector {
	selector := mggb.sql.Select()
	aggregation := make([]string, 0, len(mggb.fns))
	for _, fn := range mggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(mggb.fields)+len(mggb.fns))
		for _, f := range mggb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(mggb.fields...)...)
}

// MiningGeneralSelect is the builder for selecting fields of MiningGeneral entities.
type MiningGeneralSelect struct {
	*MiningGeneralQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (mgs *MiningGeneralSelect) Scan(ctx context.Context, v interface{}) error {
	if err := mgs.prepareQuery(ctx); err != nil {
		return err
	}
	mgs.sql = mgs.MiningGeneralQuery.sqlQuery(ctx)
	return mgs.sqlScan(ctx, v)
}

func (mgs *MiningGeneralSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := mgs.sql.Query()
	if err := mgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
